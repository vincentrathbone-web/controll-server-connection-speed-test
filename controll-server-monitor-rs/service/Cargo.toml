[package]
name = "service"
version = "0.1.7"
edition = "2021"
license = "GPL-2.0-or-later"

[[bin]]
name = "controll-server-monitor-service"
path = "src/main.rs"

[dependencies]
common = { path = "../common" }
axum = "0.7"
tokio = { version = "1", features = ["full"] }
windows-service = "0.7"
