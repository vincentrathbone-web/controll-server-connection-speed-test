[package]
name = "app"
version = "0.1.5"
edition = "2021"
license = "GPL-2.0-or-later"

[[bin]]
name = "controll-server-monitor"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-dialog = "2"
serde_json = "1"
windows-service = "0.7"
